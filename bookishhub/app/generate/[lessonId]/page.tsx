import React from 'react'

type Props = {}

const GenerateTopics = async (props: Props) => {

    return (
        <div>
            generate topics
        </div>
    );
};

export default GenerateTopics;
